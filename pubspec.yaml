name: ecoscan_ai
description: App de detección de contaminación con IA
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.0.0
  image_picker: ^1.0.0
  geolocator: ^10.0.0
  google_maps_flutter: ^2.5.0
  flutter_riverpod: ^2.0.0
  http: ^1.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true